<%- include("header"); -%>
<body>
    <div class="box" id="heading">
        <h1><%=kindOfDay%></h1>
 </div>
    
    <div class="box">
        <% for(let i=0;i<newListItem.length;i++){ %>
        <div class="items">
            <input type="checkbox" name="checkbox" value="<%=newListItem[i]._id%>" onChange="this.form.submit()" >
            <p><%= newListItem[i] %></p>

            <img src="uwp806037.png" alt="">
           
        </div>
        <% } %>
        <form action=<%=linked%> method="post" class="item">
            <input type="file" name="newItem" placeholder="New item" autocomplete="off">
            <!-- <input type="text" name="newItem" placeholder="New Item" autocomplete="off"> -->
            <button type="submit" name="Button">+</button>
        </form>   
    </div>

    

     <!-- <% newListItem.forEach(function(item){ %>

        <form action="/delete" method="post">
            <div class="item">
                
                <input type="checkbox" name="checkbox" value="<%item._id%>" onchange="this.form.submit()">
                <p><%=item.name%></p>
            </div>

        </form>


        <% }); %>
     -->








<%- include("footer"); -%>
  